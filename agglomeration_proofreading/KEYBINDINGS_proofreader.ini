[KEYBINDINGS]
select = dblclick0
get_first_sv_to_merge = keyq
set_equivalence = keyd
show_connected_partners = keyc
split_merger = control+keyx
confirm_merge_split = keyk
empty_base_vol = keyg
remove_merged_group = control+bracketright
del_sv_from_neuron = shift+keyf
add_sv_to_neuron = control+keya
set_branch_point = keyy
jump_to_last_branchpoint = digit7
remove_branchpoint = control+keyr
delete_closest_annotation = digit0
toggle_opacity_agglomeration = digit2
toggle_opacity_base = digit3
undo_last_action = control+keyz
toggle_neuron = keyn
exit_revision = control+delete
store_merger_loc = keym
save_data = control+keys
custom_toggle_xy3d = space
select_base = keyt
toggle_hover_values = keyp

[BINDING_GROUP]
select = data_view
get_first_sv_to_merge = data_view
set_equivalence = data_view
show_connected_partners = data_view
split_merger = data_view
confirm_merge_split = data_view
empty_base_vol = viewer
remove_merged_group = data_view
del_sv_from_neuron = data_view
add_sv_to_neuron = data_view
set_branch_point = viewer
jump_to_last_branchpoint = viewer
remove_branchpoint = viewer
delete_closest_annotation = data_view
toggle_opacity_agglomeration = viewer
toggle_opacity_base = viewer
undo_last_action = viewer
toggle_neuron = viewer
exit_revision = viewer
store_merger_loc = viewer
save_data = viewer
custom_toggle_xy3d = viewer
select_base = data_view
toggle_hover_values = viewer
# ATTENTION: neuroglancer uses following keyEventcodes: https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/code
# Following keys are bound in neuroglancer per default, but can be overwritten.
# For an up to date version see https://github.com/google/neuroglancer/blob/master/src/neuroglancer/ui/default_input_event_bindings.ts
# keyl: 'recolor'
# keyx: 'clear-segments'
# keys: 'toggle-show-slices'
# keyb: 'toggle-scale-bar'
# shift+keyb: 'toggle-default-annotations'
# keya: 'toggle-axis-lines'
# keyo: 'toggle-orthographic-projection'
# keyn: 'add-layer'
# keyh: 'help'
# space: 'toggle-layout'
# shift+space: 'toggle-layout-alternative'
# backslash: 'toggle-show-statistics'
# arrowleft: 'x-',
# arrowright: 'x+',
# arrowup: 'y-',
# arrowdown: 'y+',
# comma: 'z-',
# period: 'z+',
# keyz: 'snap',
# control+equal': 'zoom-in',
# control+shift+equal: 'zoom-in',
# control+minus': 'zoom-out',
# keyr: 'rotate-relative-z-',
# keye: 'rotate-relative-z+',
# shift+arrowdown: 'rotate-relative-x-',
# shift+arrowup: 'rotate-relative-x+',
# shift+arrowleft: 'rotate-relative-y-',
# shift+arrowright: 'rotate-relative-y+',
# control+wheel:'zoom-via-wheel'
# wheel: 'z+1-via-wheel'
# shift+wheel:  'z+10-via-wheel'
# dblclick0: 'select',
# control+mousedown0: 'annotate',
# mousedown2: 'move-to-mouse-position',
# control+mousedown2: 'select-annotation',
# alt+mousedown0: 'move-annotation',
# control+alt+mousedown2: 'delete-annotation',